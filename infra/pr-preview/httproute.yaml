apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: blog
spec:
  parentRefs:
    - name: eg
      namespace: envoy-gateway-system
  hostnames:
    - blog-pr-0.talos.froystein.jp
  rules:
    - backendRefs:
        - name: blog
          port: 80
