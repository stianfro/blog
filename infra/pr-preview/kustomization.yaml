apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - httproute.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: blog
      app.kubernetes.io/managed-by: argocd
    includeSelectors: false

images:
  - name: ghcr.io/stianfro/blog
    newTag: latest
