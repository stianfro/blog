// Disabled by default. The deployment may mount an environment-specific file
// at this path after analytics has been reviewed and approved.
window.froysteinAnalytics = Object.freeze({ enabled: false });
